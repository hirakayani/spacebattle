import { FireDirective } from './fire.directive';

describe('FireDirective', () => {
  it('should create an instance', () => {
    const directive = new FireDirective();
    expect(directive).toBeTruthy();
  });
});
