import { CrashDirective } from './crash.directive';

describe('CrashDirective', () => {
  it('should create an instance', () => {
    const directive = new CrashDirective();
    expect(directive).toBeTruthy();
  });
});
