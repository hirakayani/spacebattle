import { AlienDirective } from './alien.directive';

describe('AlienDirective', () => {
  it('should create an instance', () => {
    const directive = new AlienDirective();
    expect(directive).toBeTruthy();
  });
});
